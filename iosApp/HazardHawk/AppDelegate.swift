import UIKit
import SwiftUI
import shared

@main
struct HazardHawkApp: App {
    init() {
        // Initialize the shared KMP module if needed
        // Currently no specific initialization required
    }
    
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}